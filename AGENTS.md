# GuitarProMCP AI 协作规范与开发目标

本文件是供 AI/自动化协作使用的唯一规范，也是本仓库的开发目标和范围决策来源。开始任务前先阅读本文件；其他文档分别服务于用户安装、能力证据和实现细节，不重复定义这里的规则。若历史验收记录与本文件冲突，以本文件中的当前目标和范围为准。

## 协作规范

- 默认使用中文沟通和编写文档。代码、命令、路径、API、协议字段、状态值和原始界面文字保留原样。
- 用户明确要求执行修改、清理、验证或提交时，直接完成工作并报告结果；不要只停留在计划或建议。遇到阻塞时说明具体原因、已完成内容和下一步需要的外部条件。
- 开始编辑前检查 `git status` 和相关文件。保留用户已有改动，不回滚无关文件，不覆盖未授权的外部配置或数据。
- 遵循“不要过度设计，能不做就不做”。优先复用现有 C++/Qt、宿主原生接口、PowerShell 脚本和本地文档结构；只有在解决实际问题时才增加抽象、依赖或流程。
- 每项行为都区分“已实现、已验证、实验性、未实现、宿主受限”。没有实际状态、文件结果或回归证据，不得把编译成功、返回 JSON、菜单可枚举或 DLL 加载成功写成能力已完成。
- 修改后运行与风险匹配的检查，至少执行语法/格式检查和受影响的专项验证。完整原生回归需要真实 Guitar Pro 宿主时，明确说明未运行及原因。
- 提交前检查 `git diff --check`、`git status` 和敏感文件；不要提交令牌、客户端配置、临时宿主、缓存、构建产物、安装包或 `artifacts/` 内容。提交信息说明实际结果，不夸大验收范围。
- 开发目标、范围决定和协作规则只在本文件维护。README、安装说明、覆盖清单和原生开发说明通过链接引用，不复制一套新的规则。

## 产品目标

在 Windows x64 的 Guitar Pro 8.1.1.17 中提供进程内原生 C++/Qt MCP 服务：插件随宿主自动加载，在 Qt 主线程中调用已验证的宿主文档和曲谱接口，可靠操作实时文档，不依赖 Python、Node.js、输入模拟、前台窗口或外部常驻转接服务。

运行链路固定为：

```text
MCP 客户端 -> 本机 HTTP /mcp -> GuitarPro.exe 内的 C++ 插件 -> Qt/GPCore 原生文档模型
```

发布包面向最终用户提供自动加载、安装/更新/停用/启用/卸载、实例身份和客户端配置；开发者入口只用于构建、隔离启动和回归验证。

当前产品要求如下：

- 正常启动 Guitar Pro 即加载已安装插件；直接 EXE、原有快捷方式、关联曲谱和显式后台启动都必须保持各自的窗口与焦点行为。
- 正式运行的凭据和配置独立于源码目录，凭据不得写入日志；安装、更新和卸载失败必须保留普通 Guitar Pro 的可用性并可恢复。
- 手工编辑和 MCP 编辑共用真实宿主文档、未保存状态和撤销历史；多客户端不得静默指向错误曲谱或进程。
- 私有接口只对已核验的宿主文件哈希启用；增加宿主构建时必须单独验证 ABI 并记录回归证据。
- 安装前已经运行的普通实例必须重启才能加载插件；热附加不属于交付前提。

实现约束：复用宿主事件循环和已有状态工具，修改必须在 Qt 主线程按顺序执行；没有实际需求时不增加外部常驻服务、脚本运行时、输入模拟或前台窗口依赖。

## 阶段目标

P0-P10 已按下表声明的范围完成。P9 已验收可核验的节拍文本、力度标记、符干方向、谱号、沿用的 P8 能力和只读能力矩阵；截图中尚未具备可靠原生写入路径的剩余编辑要求统一归入 P9 的保留清单，并明确标记为宿主受限、实验性或未实现。P10 复用 Qt 偏好模型完成已确认可读写的全局偏好及基础音频/MIDI 状态，已验证重启持久化和新建曲谱默认资讯继承。P11 已交付最小 Qt 离屏截图路径，真实宿主全矩阵仍标记为实验性或宿主受限。P12 已完成窗口枚举与指定截图的核心实现和真实宿主专项：窗口数量、稳定身份、状态、模态下显式选择及只读性有证据；未构造的特殊窗口组合仍保留为实验性、宿主受限或未验证。P13 已完成最小音频 Provider 契约和消费者专项；P14 仅建立端到端实时音频流监测与管控计划，尚未实现或验收。验收记录及剩余要求见 [docs/DEVELOPMENT_PLAN.md](docs/DEVELOPMENT_PLAN.md)；阶段状态和具名证据见 [docs/COVERAGE.md](docs/COVERAGE.md)。

| 阶段 | 目标与当前范围 | 明确保留的边界 |
| --- | --- | --- |
| P0 自动加载 | 在隔离宿主中验证 Qt 自动加载入口、直接 EXE、快捷方式、关联曲谱和移除恢复 | 不替换宿主 EXE 或 Qt DLL；热附加不是首版前提 |
| P1 安装集成 | 预编译包、真实目录安装/更新/启停/卸载/重装、配置保留、可见/后台入口和普通退出 | 启动后快速退出的 AMNetwork 等待按宿主限制保留 |
| P2 会话与文档 | 实例身份、端口冲突、双客户端、连接重启失效、打开/新建/关闭/保存/恢复及模态处理 | 独立 GUI 多开、原生标签拖动、原生保存进度取消按宿主限制保留 |
| P3 曲谱与音轨 | 必要范围的音符/节拍技法、混合乐器、调弦/变调夹、移调、连接链和复杂反复 | 任意乐器定义、排版搜索和完整技法组合不属于本阶段 |
| P4 选区与内容转移 | 选区筛选、批量节拍编辑、插件独立剪贴板、特别/重复粘贴、跨小节转移和兼容性拒绝 | 系统剪贴板互通、任意音轨集合映射和部分小节跨栏替换保留为扩展 |
| P5 播放与音频 | 速度点/渐变、展开时间线、循环、播放定位、已有音色/效果、设备选项和 PCM 验证 | 任意音色/效果定义、ASIO/驱动故障和全部声学组合不在范围 |
| P6 文件交换与工作区 | GP5/GPX/MusicXML/MIDI 交换、PDF/PNG/WAV 输出、页面/谱表和明确作用域偏好 | 物理打印队列、其他编码器、任意样式和完整权限受限回归不在范围 |
| P7 发布验收 | 最终包完整回归、双客户端、十份文档、一小时长测、资源回落、恢复、退出和真实安装生命周期 | 以支持版本和发布证据为准，不扩展未验收宿主或已排除功能 |
| P8 高层编曲与曲谱语义 | 批量建谱与一次撤销、JSON 语义交换、文本六线谱、歌曲结构、和弦/和弦图、五行歌词、段落及页面元数据；通过真实宿主专项和完整原生回归 | JSON v1 限 32 轨、256 小节、20000 拍，不是完整 GP 文件备份；ASCII 不可逆；任意乐器、自动指法搜索、全部自定义音程/技法组合和完整歌词/刻谱排版不在验收范围 |
| P9 编辑面板剩余能力 | 已完成可核验范围：节拍文本、`PPP`–`FFF` 力度标记、`G2/F4/C3` 谱号的光标/选区或小节写入，Unicode 与空白保留，原生撤销重做、保存重开；沿用 P8 的和弦/歌词/页面元数据及 P5 的已有音色效果和速度自动化；新增整轨自动化读取及保留既有点/旁路的实验性 DSP 参数路径；提供 `gp_p9_status` 能力矩阵 | P9 继续归档并管理剩余要求：力度清除、细粒度符杠/括号/连音括号/分组/间距、力度/表情/音量自动化、完整歌词/刻谱排版、任意乐器/指法、自定义音色/效果、完整自动化、系统剪贴板、任意音轨映射、剩余特别粘贴过滤项及全部技法组合；分别标记为宿主受限、实验性或未实现，见 [P9 剩余要求](docs/DEVELOPMENT_PLAN.md#p9-剩余要求归档) |
| P10 偏好设置与基础音频/MIDI 控制 | 复用现有 `gp_preferences` 和 `gp_audio_device`，通过显式 Qt 模型/属性 allowlist 读取并设置一般、界面、乐谱错误、我的资讯、MIDI 偏好及音频 choices；核对实际读回、恢复、跨宿主重启持久化和新建曲谱默认资讯继承 | 更新/Beta、每路 MIDI 延迟、通道检测和驱动控制面板按宿主受限或待调查保留；未提供的模型属性、额外声道映射与设备 choices 不作猜测 |
| P11 界面截图与窗口状态采集 | 已实现最小 Qt 离屏路径，实验性：新增 `gp_screenshot`，在不激活、不抢焦点、不发送输入的前提下获取 Guitar Pro 当前窗口或活动模态对话框；通过 MCP image content 返回 PNG，并覆盖后台、被遮挡和最小化状态 | 不截取整个桌面；最小化/被遮挡时不能依赖桌面合成像素，真实宿主等价性仍待补验；无法可靠捕获的窗口必须显式返回 `status=host_limited`，不得伪造成功 |
| P12 多窗口枚举与指定窗口截图 | 已实现并通过核心真实宿主专项：只读 `gp_windows` 返回数量、稳定 ID、父关系与状态；`gp_screenshot(window_id)` 在多窗口和活动模态存在时保留显式目标；主窗口、非模态偏好、键盘/指板浮动窗口与关闭确认已有证据，截图保持实验性 | 范围见下方约定；默认截图保留 P11 行为；失效或跨实例 ID 明确拒绝，不回退到其他窗口；各类窗口渲染须分别核验，枚举成功不代表截图已验证 |
| P13 音频 Provider 与多插件接口设计 | 已完成最小进程内 Provider 契约、MCP 适配、generation 失效和独立原生消费者专项；VST3 消费者未实现 | `audio_bridge_api.h` v1 明确结构体大小、宿主哈希、能力位、状态码、generation 和 Qt 控制线程；实时 PCM 获取、系统混音采集和跨线程保存宿主裸指针不因桥接交付自动纳入范围 |
| P14 端到端实时音频流监测与管控 | 计划中，尚未实现或验收 | 以宿主实际可核验的实时输入/输出 tap 为前提，覆盖宿主音频源、音轨、效果链、混音、格式转换和 Standard/ASIO 端点的指标、诊断、受控操作与恢复；两种后端共用 Provider 和中间模型，但实际拓扑、回调、时钟和错误必须分别核验；没有已核验 tap 的宿主必须返回 `host_limited`，不得用离线 PCM 或设备配置读回冒充实时覆盖 |

### P12 范围约定

- 窗口范围为当前 MCP 实例可确认归属的 Qt 主窗口、模态/非模态对话框、工具/浮动窗口、弹出窗口及具有独立窗口语义的子窗口。普通按钮、面板和嵌入式控件随所属窗口渲染，不单独计入窗口数量；实际存在的 `Qt::SubWindow` 需单独识别。
- 数量与状态必须明确统计范围，区分可见和隐藏窗口；最小化、最大化、活动状态、模态状态和父窗口关系读取实际 Qt 状态，不从标题或窗口数量猜测。未发现、未构造的窗口不计数，列表不完整时明确说明。
- 每个已枚举窗口具有绑定实例和对象生命周期的稳定 ID。多客户端重复查询、标题变化、隐藏或最小化不使 ID 改指其他窗口；窗口对象销毁或宿主重启后旧 ID 失效。
- 不传 `window_id` 时保持 P11 的活动模态优先、否则主窗口行为；显式指定时只捕获该窗口，即使主窗口正被模态对话框阻塞。截图与枚举都是只读能力，不改变焦点、窗口可见性、模态关系、文档状态或撤销历史。
- 继续使用进程内 Qt 路径和标准 MCP PNG image content，不跨进程、不截取桌面。没有可靠 Qt 渲染路径的窗口须返回 `status=host_limited`；纯系统原生窗口、外部驱动窗口及特殊渲染窗口不承诺完整覆盖，必须记录可枚举性与可渲染性的实际边界。

P13 的实现归属保持可替换：MCP 插件可以先作为首个 Provider 实现，后续也可以迁移到独立插件；实时宿主对象图必须有明确的权威绑定来源，消费者不得复制一套 `Score`/`Track`/`Musician`/`Sound`/`EffectsChain` 解析。P13 仍使用进程内 Qt/GPCore 链路，但允许在明确版本和迁移说明后调整 MCP 工具、字段和数据模型；详细计划、阶段交付物和验收矩阵见 [docs/DEVELOPMENT_PLAN.md](docs/DEVELOPMENT_PLAN.md#p13音频-provider-与多插件接口设计计划)。

P14 的范围约定：宿主已经拥有且能从已核验实时回调观察到的输入流、曲谱/RSE/MIDI 源、音轨效果、总线/主混音、格式转换和 Standard/ASIO 输入输出端点均纳入；Standard 与 ASIO 的共用中间路径必须由实时指标和拓扑证据确认，不能按名称假设相同。实时监测必须使用独立流 ID、generation、固定容量缓冲和控制线程状态机，分开报告宿主音频丢帧与监测缓冲区溢出。系统混音、单独的操作系统麦克风采集、其他进程、厂商控制面板和操作系统级驱动修复不属于默认进程内范围；没有宿主 tap、线程契约或真实设备证据时只标记为未实现、实验性或 `host_limited`。详细阶段计划见 [docs/DEVELOPMENT_PLAN.md](docs/DEVELOPMENT_PLAN.md#p14-端到端实时音频流监测与管控计划)。

## 质量门槛

- 私有 ABI 只对已核验的宿主文件哈希启用；不支持的版本必须明确拒绝。
- 原生异步请求必须观察到真实终态。结果未知时保留写入阻塞，不自动重放编辑或猜测回滚成功。
- 修改必须核对目标隔离、未保存状态、撤销/重做、文件字节和保存重开结果；影响播放的修改还要核对时间线或音频结果。
- 安装、更新和卸载必须检查文件归属并可回滚；用户配置、凭据、宿主文件和快捷方式不应被无关修改。
- 运行令牌、连接配置、临时宿主和生成证据留在被 `.gitignore` 忽略的本地目录。提交内容只包含可复现的源码、脚本、配置模板和必要文档。

## 文档分工

- [README.md](README.md)：项目概览、用户入口、MCP 工具范围概览和开发启动。
- [docs/INSTALL.md](docs/INSTALL.md)：安装包使用、更新/卸载和故障恢复。
- [docs/COVERAGE.md](docs/COVERAGE.md)：已验证能力、保留边界和证据索引。
- [native/README.md](native/README.md)：C++/Qt 构建、加载、ABI 和测试命令；协议与工具行为见 [native/API.md](native/API.md)，P8 格式与调用细节见 [native/P8.md](native/P8.md)。
- [docs/DEVELOPMENT_PLAN.md](docs/DEVELOPMENT_PLAN.md)：P0-P14 阶段总览、P9 保留边界、P10 验收、P11 实验性记录、P12 验收记录、P13 音频 Provider 接口设计计划与 P14 实时音频流监测管控计划；历史记录见文末归档，当前目标以本文件为准。
- [docs/README.md](docs/README.md)：面向用户和开发者的文档导航。
